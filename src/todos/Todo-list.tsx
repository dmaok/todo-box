// import {getTodosRequest} from "../rest/todo";
// import {useEffect} from "react";

export const TodoList = () => {
  // useEffect(() => {
  //   getTodosRequest().then(r => console.log(r)).catch(console.log)
  // }, []);

  return (
    <span>"todo list"</span>
  )
}
